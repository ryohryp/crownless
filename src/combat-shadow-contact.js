(() => {
  "use strict";

  /*
   * Kept as a compatibility no-op for existing script order.
   * Actor shadow contact is now owned directly by drawGroundShadow() in
   * combat-manuscript-render.js so CanvasRenderingContext2D is not patched.
   */
})();
